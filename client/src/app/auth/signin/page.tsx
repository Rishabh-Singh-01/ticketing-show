import { SigninForm } from '@/components/custom/non-common/signin-form';

export default function Signup() {
  return (
    <div className='flex min-h-screen flex-col items-center justify-between p-24'>
      <SigninForm />
    </div>
  );
}
